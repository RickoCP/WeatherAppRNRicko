export interface ISelectedTerritoryEntity {
  name: string;
  region: string;
  country: string;
  url: string;
}

export interface ISelectedTerritoryData {
  name: string;
  region: string;
  country: string;
  url: string;
}
